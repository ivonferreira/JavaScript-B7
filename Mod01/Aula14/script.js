let idade = 20;
if (idade ===20){ // == verifica se os caracteres são iguais, === verifica se é o mesmo tipo e caracteres, podemos usar tb !==
    idade = idade + 20
    console.log('Idade adicionada com sucesso');console.log(idade);
}// O if não precisa obrigatóriamente do else, caso não satisfaça a condição não executará nada então